spring:
  application:
    name: notification-service
    #    RABBITMQ
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 8083

rabbitmq:
  user:
    group: users-group
    exchange: user.exchange
    all:
      queue: user.all.queue
      routing_key: user.*

logging:
  level:
    pl:
      alex: DEBUG
