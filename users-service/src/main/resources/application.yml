spring:
  application:
    name: users-service
  #    RABBITMQ
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 8081

rabbitmq:
  user:
    group: users-group
    exchange: user.exchange
    created:
      key: user.created

logging:
  level:
    pl:
      alex: DEBUG
